//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by StorageClassGenerator
//
//     Do not change code in this file, it will get lost when the file gets 
//     auto generated again. Write your code into SingleChildParentR.cs.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
using System;
using System.Collections.Generic;
using System.Threading;
using StorageLib;


namespace TestContext  {


  public partial class SingleChildParentR: IStorageItemGeneric<SingleChildParentR> {

    #region Properties
    //      ----------

    /// <summary>
    /// Unique identifier for SingleChildParentR. Gets set once SingleChildParentR gets added to DC.Data.
    /// </summary>
    public int Key { get; private set; }
    internal static void SetKey(IStorageItem singleChildParentR, int key, bool isRollback) {
#if DEBUG
      if (isRollback) {
        if (key==StorageExtensions.NoKey) {
          DC.Trace?.Invoke($"Release SingleChildParentR key @{singleChildParentR.Key} #{singleChildParentR.GetHashCode()}");
        } else {
          DC.Trace?.Invoke($"Store SingleChildParentR key @{key} #{singleChildParentR.GetHashCode()}");
        }
      }
#endif
      ((SingleChildParentR)singleChildParentR).Key = key;
    }


    public string Text { get; private set; }


    public SingleChildChild? Child { get; private set; }


    /// <summary>
    /// Headers written to first line in CSV file
    /// </summary>
    internal static readonly string[] Headers = {"Key", "Text"};


    /// <summary>
    /// None existing SingleChildParentR
    /// </summary>
    internal static SingleChildParentR NoSingleChildParentR = new SingleChildParentR("NoText", isStoring: false);
    #endregion


    #region Events
    //      ------

    /// <summary>
    /// Content of SingleChildParentR has changed. Gets only raised for changes occurring after loading DC.Data with previously stored data.
    /// </summary>
    public event Action</*old*/SingleChildParentR, /*new*/SingleChildParentR>? HasChanged;
    #endregion


    #region Constructors
    //      ------------

    /// <summary>
    /// SingleChildParentR Constructor. If isStoring is true, adds SingleChildParentR to DC.Data.SingleChildParentRs.
    /// </summary>
    public SingleChildParentR(string text, bool isStoring = true) {
      Key = StorageExtensions.NoKey;
      Text = text;
#if DEBUG
      DC.Trace?.Invoke($"new SingleChildParentR: {ToTraceString()}");
#endif
      onConstruct();
      if (DC.Data.IsTransaction) {
        DC.Data.AddTransaction(new TransactionItem(7,TransactionActivityEnum.New, Key, this));
      }

      if (isStoring) {
        Store();
      }
    }
    partial void onConstruct();


    /// <summary>
    /// Cloning constructor. It will copy all data from original except any collection (children).
    /// </summary>
    #pragma warning disable CS8618 // Children collections are uninitialized.
    public SingleChildParentR(SingleChildParentR original) {
    #pragma warning restore CS8618 //
      Key = StorageExtensions.NoKey;
      Text = original.Text;
      onCloned(this);
    }
    partial void onCloned(SingleChildParentR clone);


    /// <summary>
    /// Constructor for SingleChildParentR read from CSV file
    /// </summary>
    private SingleChildParentR(int key, CsvReader csvReader){
      Key = key;
      Text = csvReader.ReadString();
      onCsvConstruct();
    }
    partial void onCsvConstruct();


    /// <summary>
    /// New SingleChildParentR read from CSV file
    /// </summary>
    internal static SingleChildParentR Create(int key, CsvReader csvReader) {
      return new SingleChildParentR(key, csvReader);
    }
    #endregion


    #region Methods
    //      -------

    /// <summary>
    /// Adds SingleChildParentR to DC.Data.SingleChildParentRs.<br/>
    /// Throws an Exception when SingleChildParentR is already stored.
    /// </summary>
    public void Store() {
      if (Key>=0) {
        throw new Exception($"SingleChildParentR cannot be stored again in DC.Data, key {Key} is greater equal 0." + Environment.NewLine + ToString());
      }

      var isCancelled = false;
      onStoring(ref isCancelled);
      if (isCancelled) return;

      DC.Data._SingleChildParentRs.Add(this);
      onStored();
#if DEBUG
      DC.Trace?.Invoke($"Stored SingleChildParentR #{GetHashCode()} @{Key}");
#endif
    }
    partial void onStoring(ref bool isCancelled);
    partial void onStored();


    /// <summary>
    /// Estimated number of UTF8 characters needed to write SingleChildParentR to CSV file
    /// </summary>
    public const int EstimatedLineLength = 150;


    /// <summary>
    /// Write SingleChildParentR to CSV file
    /// </summary>
    internal static void Write(SingleChildParentR singleChildParentR, CsvWriter csvWriter) {
      singleChildParentR.onCsvWrite();
      csvWriter.Write(singleChildParentR.Text);
    }
    partial void onCsvWrite();


    /// <summary>
    /// Updates SingleChildParentR with the provided values
    /// </summary>
    public void Update(string text) {
      var clone = new SingleChildParentR(this);
      var isCancelled = false;
      onUpdating(text, ref isCancelled);
      if (isCancelled) return;

#if DEBUG
      DC.Trace?.Invoke($"Updating SingleChildParentR: {ToTraceString()}");
#endif

      //update properties and detect if any value has changed
      var isChangeDetected = false;
      if (Text!=text) {
        Text = text;
        isChangeDetected = true;
      }
      if (isChangeDetected) {
        onUpdated(clone);
        if (Key>=0) {
          DC.Data._SingleChildParentRs.ItemHasChanged(clone, this);
        } else if (DC.Data.IsTransaction) {
          DC.Data.AddTransaction(new TransactionItem(7, TransactionActivityEnum.Update, Key, this, oldItem: clone));
        }
        HasChanged?.Invoke(clone, this);
      }
#if DEBUG
      DC.Trace?.Invoke($"Updated SingleChildParentR: {ToTraceString()}");
#endif
    }
    partial void onUpdating(string text, ref bool isCancelled);
    partial void onUpdated(SingleChildParentR old);


    /// <summary>
    /// Updates this SingleChildParentR with values from CSV file
    /// </summary>
    internal static void Update(SingleChildParentR singleChildParentR, CsvReader csvReader){
      singleChildParentR.Text = csvReader.ReadString();
      singleChildParentR.onCsvUpdate();
    }
    partial void onCsvUpdate();


    /// <summary>
    /// Add singleChildChild to Child.
    /// </summary>
    internal void AddToChild(SingleChildChild singleChildChild) {
#if DEBUG
      if (singleChildChild==SingleChildChild.NoSingleChildChild) throw new Exception();
      if ((singleChildChild.Key>=0)&&(Key<0)) throw new Exception();
      if(Child==singleChildChild) throw new Exception();
#endif
      if (Child!=null) {
        throw new Exception($"SingleChildParentR.AddToChild(): '{Child}' is already assigned to Child, it is not possible to assign now '{singleChildChild}'.");
      }
      Child = singleChildChild;
      onAddedToChild(singleChildChild);
#if DEBUG
      DC.Trace?.Invoke($"Add SingleChildChild {singleChildChild.GetKeyOrHash()} to " +
        $"{this.GetKeyOrHash()} SingleChildParentR.Child");
#endif
    }
    partial void onAddedToChild(SingleChildChild singleChildChild);


    /// <summary>
    /// Removes singleChildChild from SingleChildParentR.
    /// </summary>
    internal void RemoveFromChild(SingleChildChild singleChildChild) {
#if DEBUG
      if (Child!=singleChildChild) {
        throw new Exception($"SingleChildParentR.RemoveFromChild(): Child does not link to singleChildChild '{singleChildChild}' but '{Child}'.");
      }
#endif
      Child = null;
      onRemovedFromChild(singleChildChild);
#if DEBUG
      DC.Trace?.Invoke($"Remove SingleChildChild {singleChildChild.GetKeyOrHash()} from " +
        $"{this.GetKeyOrHash()} SingleChildParentR.Child");
#endif
    }
    partial void onRemovedFromChild(SingleChildChild singleChildChild);


    /// <summary>
    /// Removes SingleChildParentR from DC.Data.SingleChildParentRs.
    /// </summary>
    public void Release() {
      if (Key<0) {
        throw new Exception($"SingleChildParentR.Release(): SingleChildParentR '{this}' is not stored in DC.Data, key is {Key}.");
      }
      if (Child?.Key>=0) {
        throw new Exception($"Cannot release SingleChildParentR '{this}' " + Environment.NewLine + 
          $"because '{Child}' in SingleChildParentR.Child is still stored.");
      }
      onReleased();
      DC.Data._SingleChildParentRs.Remove(Key);
#if DEBUG
      DC.Trace?.Invoke($"Released SingleChildParentR @{Key} #{GetHashCode()}");
#endif
    }
    partial void onReleased();


    /// <summary>
    /// Undoes the new() statement as part of a transaction rollback.
    /// </summary>
    internal static void RollbackItemNew(IStorageItem item) {
      var singleChildParentR = (SingleChildParentR) item;
#if DEBUG
      DC.Trace?.Invoke($"Rollback new SingleChildParentR(): {singleChildParentR.ToTraceString()}");
#endif
      singleChildParentR.onRollbackItemNew();
    }
    partial void onRollbackItemNew();


    /// <summary>
    /// Releases SingleChildParentR from DC.Data.SingleChildParentRs as part of a transaction rollback of Store().
    /// </summary>
    internal static void RollbackItemStore(IStorageItem item) {
      var singleChildParentR = (SingleChildParentR) item;
#if DEBUG
      DC.Trace?.Invoke($"Rollback SingleChildParentR.Store(): {singleChildParentR.ToTraceString()}");
#endif
      singleChildParentR.onRollbackItemStored();
    }
    partial void onRollbackItemStored();


    /// <summary>
    /// Restores the SingleChildParentR item data as it was before the last update as part of a transaction rollback.
    /// </summary>
    internal static void RollbackItemUpdate(IStorageItem oldStorageItem, IStorageItem newStorageItem) {
      var oldItem = (SingleChildParentR) oldStorageItem;//an item clone with the values before item was updated
      var item = (SingleChildParentR) newStorageItem;//is the instance whose values should be restored
#if DEBUG
      DC.Trace?.Invoke($"Rolling back SingleChildParentR.Update(): {item.ToTraceString()}");
#endif

      // updated item: restore old values
      item.Text = oldItem.Text;
      item.onRollbackItemUpdated(oldItem);
#if DEBUG
      DC.Trace?.Invoke($"Rolled back SingleChildParentR.Update(): {item.ToTraceString()}");
#endif
    }
    partial void onRollbackItemUpdated(SingleChildParentR oldSingleChildParentR);


    /// <summary>
    /// Adds SingleChildParentR to DC.Data.SingleChildParentRs as part of a transaction rollback of Release().
    /// </summary>
    internal static void RollbackItemRelease(IStorageItem item) {
      var singleChildParentR = (SingleChildParentR) item;
#if DEBUG
      DC.Trace?.Invoke($"Rollback SingleChildParentR.Release(): {singleChildParentR.ToTraceString()}");
#endif
      singleChildParentR.onRollbackItemRelease();
    }
    partial void onRollbackItemRelease();


    /// <summary>
    /// Returns property values for tracing. Parents are shown with their key instead their content.
    /// </summary>
    public string ToTraceString() {
      var returnString =
        $"{this.GetKeyOrHash()}|" +
        $" {Text}";
      onToTraceString(ref returnString);
      return returnString;
    }
    partial void onToTraceString(ref string returnString);


    /// <summary>
    /// Returns property values
    /// </summary>
    public string ToShortString() {
      var returnString =
        $"{Key.ToKeyString()}," +
        $" {Text}";
      onToShortString(ref returnString);
      return returnString;
    }
    partial void onToShortString(ref string returnString);


    /// <summary>
    /// Returns all property names and values
    /// </summary>
    public override string ToString() {
      var returnString =
        $"Key: {Key.ToKeyString()}," +
        $" Text: {Text}," +
        $" Child: {Child?.ToShortString()};";
      onToString(ref returnString);
      return returnString;
    }
    partial void onToString(ref string returnString);
    #endregion
  }
}
