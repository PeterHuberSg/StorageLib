//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by StorageClassGenerator
//
//     Do not change code in this file, it will get lost when the file gets 
//     auto generated again. Write your code into LookupParentNR.cs.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
using System;
using System.Collections.Generic;
using System.Threading;
using StorageLib;


namespace StorageDataContext  {


  public partial class LookupParentNR: IStorageItemGeneric<LookupParentNR> {

    #region Properties
    //      ----------

    /// <summary>
    /// Unique identifier for LookupParentNR. Gets set once LookupParentNR gets added to DC.Data.
    /// </summary>
    public int Key { get; private set; }
    internal static void SetKey(IStorageItem lookupParentNR, int key, bool isRollback) {
#if DEBUG
      if (isRollback) {
        if (key==StorageExtensions.NoKey) {
          DC.Trace?.Invoke($"Release LookupParentNR key @{lookupParentNR.Key} #{lookupParentNR.GetHashCode()}");
        } else {
          DC.Trace?.Invoke($"Store LookupParentNR key @{key} #{lookupParentNR.GetHashCode()}");
        }
      }
#endif
      ((LookupParentNR)lookupParentNR).Key = key;
    }


    public string Text { get; private set; }


    /// <summary>
    /// Headers written to first line in CSV file
    /// </summary>
    internal static readonly string[] Headers = {"Key", "Text"};


    /// <summary>
    /// None existing LookupParentNR
    /// </summary>
    internal static LookupParentNR NoLookupParentNR = new LookupParentNR("NoText", isStoring: false);
    #endregion


    #region Events
    //      ------

    /// <summary>
    /// Content of LookupParentNR has changed. Gets only raised for changes occurring after loading DC.Data with previously stored data.
    /// </summary>
    public event Action</*old*/LookupParentNR, /*new*/LookupParentNR>? HasChanged;
    #endregion


    #region Constructors
    //      ------------

    /// <summary>
    /// LookupParentNR Constructor. If isStoring is true, adds LookupParentNR to DC.Data.LookupParentNRs.
    /// </summary>
    public LookupParentNR(string text, bool isStoring = true) {
      Key = StorageExtensions.NoKey;
      Text = text;
#if DEBUG
      DC.Trace?.Invoke($"new LookupParentNR: {ToTraceString()}");
#endif
      onConstruct();
      if (DC.Data.IsTransaction) {
        DC.Data.AddTransaction(new TransactionItem(3,TransactionActivityEnum.New, Key, this));
      }

      if (isStoring) {
        Store();
      }
    }
    partial void onConstruct();


    /// <summary>
    /// Cloning constructor. It will copy all data from original except any collection (children).
    /// </summary>
    #pragma warning disable CS8618 // Children collections are uninitialized.
    public LookupParentNR(LookupParentNR original) {
    #pragma warning restore CS8618 //
      Key = StorageExtensions.NoKey;
      Text = original.Text;
      onCloned(this);
    }
    partial void onCloned(LookupParentNR clone);


    /// <summary>
    /// Constructor for LookupParentNR read from CSV file
    /// </summary>
    private LookupParentNR(int key, CsvReader csvReader){
      Key = key;
      Text = csvReader.ReadString();
      onCsvConstruct();
    }
    partial void onCsvConstruct();


    /// <summary>
    /// New LookupParentNR read from CSV file
    /// </summary>
    internal static LookupParentNR Create(int key, CsvReader csvReader) {
      return new LookupParentNR(key, csvReader);
    }
    #endregion


    #region Methods
    //      -------

    /// <summary>
    /// Adds LookupParentNR to DC.Data.LookupParentNRs.<br/>
    /// Throws an Exception when LookupParentNR is already stored.
    /// </summary>
    public void Store() {
      if (Key>=0) {
        throw new Exception($"LookupParentNR cannot be stored again in DC.Data, key {Key} is greater equal 0." + Environment.NewLine + ToString());
      }

      var isCancelled = false;
      onStoring(ref isCancelled);
      if (isCancelled) return;

      DC.Data._LookupParentNRs.Add(this);
      onStored();
#if DEBUG
      DC.Trace?.Invoke($"Stored LookupParentNR #{GetHashCode()} @{Key}");
#endif
    }
    partial void onStoring(ref bool isCancelled);
    partial void onStored();


    /// <summary>
    /// Estimated number of UTF8 characters needed to write LookupParentNR to CSV file
    /// </summary>
    public const int EstimatedLineLength = 150;


    /// <summary>
    /// Write LookupParentNR to CSV file
    /// </summary>
    internal static void Write(LookupParentNR lookupParentNR, CsvWriter csvWriter) {
      lookupParentNR.onCsvWrite();
      csvWriter.Write(lookupParentNR.Text);
    }
    partial void onCsvWrite();


    /// <summary>
    /// Updates LookupParentNR with the provided values
    /// </summary>
    public void Update(string text) {
      var clone = new LookupParentNR(this);
      var isCancelled = false;
      onUpdating(text, ref isCancelled);
      if (isCancelled) return;

#if DEBUG
      DC.Trace?.Invoke($"Updating LookupParentNR: {ToTraceString()}");
#endif

      //update properties and detect if any value has changed
      var isChangeDetected = false;
      if (Text!=text) {
        Text = text;
        isChangeDetected = true;
      }
      if (isChangeDetected) {
        onUpdated(clone);
        if (Key>=0) {
          DC.Data._LookupParentNRs.ItemHasChanged(clone, this);
        } else if (DC.Data.IsTransaction) {
          DC.Data.AddTransaction(new TransactionItem(3, TransactionActivityEnum.Update, Key, this, oldItem: clone));
        }
        HasChanged?.Invoke(clone, this);
      }
#if DEBUG
      DC.Trace?.Invoke($"Updated LookupParentNR: {ToTraceString()}");
#endif
    }
    partial void onUpdating(string text, ref bool isCancelled);
    partial void onUpdated(LookupParentNR old);


    /// <summary>
    /// Updates this LookupParentNR with values from CSV file
    /// </summary>
    internal static void Update(LookupParentNR lookupParentNR, CsvReader csvReader){
      lookupParentNR.Text = csvReader.ReadString();
      lookupParentNR.onCsvUpdate();
    }
    partial void onCsvUpdate();


    /// <summary>
    /// Releasing LookupParentNR from DC.Data.LookupParentNRs is not supported.
    /// </summary>
    public void Release() {
      throw new NotSupportedException("Release() is not supported, StorageClass attribute AreInstancesReleasable is false.");
    }


    /// <summary>
    /// Undoes the new() statement as part of a transaction rollback.
    /// </summary>
    internal static void RollbackItemNew(IStorageItem item) {
      var lookupParentNR = (LookupParentNR) item;
#if DEBUG
      DC.Trace?.Invoke($"Rollback new LookupParentNR(): {lookupParentNR.ToTraceString()}");
#endif
      lookupParentNR.onRollbackItemNew();
    }
    partial void onRollbackItemNew();


    /// <summary>
    /// Releases LookupParentNR from DC.Data.LookupParentNRs as part of a transaction rollback of Store().
    /// </summary>
    internal static void RollbackItemStore(IStorageItem item) {
      var lookupParentNR = (LookupParentNR) item;
#if DEBUG
      DC.Trace?.Invoke($"Rollback LookupParentNR.Store(): {lookupParentNR.ToTraceString()}");
#endif
      lookupParentNR.onRollbackItemStored();
    }
    partial void onRollbackItemStored();


    /// <summary>
    /// Restores the LookupParentNR item data as it was before the last update as part of a transaction rollback.
    /// </summary>
    internal static void RollbackItemUpdate(IStorageItem oldStorageItem, IStorageItem newStorageItem) {
      var oldItem = (LookupParentNR) oldStorageItem;//an item clone with the values before item was updated
      var item = (LookupParentNR) newStorageItem;//is the instance whose values should be restored
#if DEBUG
      DC.Trace?.Invoke($"Rolling back LookupParentNR.Update(): {item.ToTraceString()}");
#endif

      // updated item: restore old values
      item.Text = oldItem.Text;
      item.onRollbackItemUpdated(oldItem);
#if DEBUG
      DC.Trace?.Invoke($"Rolled back LookupParentNR.Update(): {item.ToTraceString()}");
#endif
    }
    partial void onRollbackItemUpdated(LookupParentNR oldLookupParentNR);


    /// <summary>
    /// Adds LookupParentNR to DC.Data.LookupParentNRs as part of a transaction rollback of Release().
    /// </summary>
    internal static void RollbackItemRelease(IStorageItem item) {
      var lookupParentNR = (LookupParentNR) item;
#if DEBUG
      DC.Trace?.Invoke($"Rollback LookupParentNR.Release(): {lookupParentNR.ToTraceString()}");
#endif
      lookupParentNR.onRollbackItemRelease();
    }
    partial void onRollbackItemRelease();


    /// <summary>
    /// Returns property values for tracing. Parents are shown with their key instead their content.
    /// </summary>
    public string ToTraceString() {
      var returnString =
        $"{this.GetKeyOrHash()}|" +
        $" {Text}";
      onToTraceString(ref returnString);
      return returnString;
    }
    partial void onToTraceString(ref string returnString);


    /// <summary>
    /// Returns property values
    /// </summary>
    public string ToShortString() {
      var returnString =
        $"{Key.ToKeyString()}," +
        $" {Text}";
      onToShortString(ref returnString);
      return returnString;
    }
    partial void onToShortString(ref string returnString);


    /// <summary>
    /// Returns all property names and values
    /// </summary>
    public override string ToString() {
      var returnString =
        $"Key: {Key.ToKeyString()}," +
        $" Text: {Text};";
      onToString(ref returnString);
      return returnString;
    }
    partial void onToString(ref string returnString);
    #endregion
  }
}
