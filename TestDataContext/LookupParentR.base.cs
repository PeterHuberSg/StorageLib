//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by StorageClassGenerator
//
//     Do not change code in this file, it will get lost when the file gets 
//     auto generated again. Write your code into LookupParentR.cs.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
using System;
using System.Collections.Generic;
using System.Threading;
using StorageLib;


namespace TestContext  {


  public partial class LookupParentR: IStorageItemGeneric<LookupParentR> {

    #region Properties
    //      ----------

    /// <summary>
    /// Unique identifier for LookupParentR. Gets set once LookupParentR gets added to DC.Data.
    /// </summary>
    public int Key { get; private set; }
    internal static void SetKey(IStorageItem lookupParentR, int key, bool isRollback) {
#if DEBUG
      if (isRollback) {
        if (key==StorageExtensions.NoKey) {
          DC.Trace?.Invoke($"Release LookupParentR key @{lookupParentR.Key} #{lookupParentR.GetHashCode()}");
        } else {
          DC.Trace?.Invoke($"Store LookupParentR key @{key} #{lookupParentR.GetHashCode()}");
        }
      }
#endif
      ((LookupParentR)lookupParentR).Key = key;
    }


    public string Text { get; private set; }


    /// <summary>
    /// Headers written to first line in CSV file
    /// </summary>
    internal static readonly string[] Headers = {"Key", "Text"};


    /// <summary>
    /// None existing LookupParentR
    /// </summary>
    internal static LookupParentR NoLookupParentR = new LookupParentR("NoText", isStoring: false);
    #endregion


    #region Events
    //      ------

    /// <summary>
    /// Content of LookupParentR has changed. Gets only raised for changes occurring after loading DC.Data with previously stored data.
    /// </summary>
    public event Action</*old*/LookupParentR, /*new*/LookupParentR>? HasChanged;
    #endregion


    #region Constructors
    //      ------------

    /// <summary>
    /// LookupParentR Constructor. If isStoring is true, adds LookupParentR to DC.Data.LookupParentRs.
    /// </summary>
    public LookupParentR(string text, bool isStoring = true) {
      Key = StorageExtensions.NoKey;
      Text = text;
#if DEBUG
      DC.Trace?.Invoke($"new LookupParentR: {ToTraceString()}");
#endif
      onConstruct();
      if (DC.Data.IsTransaction) {
        DC.Data.AddTransaction(new TransactionItem(2,TransactionActivityEnum.New, Key, this));
      }

      if (isStoring) {
        Store();
      }
    }
    partial void onConstruct();


    /// <summary>
    /// Cloning constructor. It will copy all data from original except any collection (children).
    /// </summary>
    #pragma warning disable CS8618 // Children collections are uninitialized.
    public LookupParentR(LookupParentR original) {
    #pragma warning restore CS8618 //
      Key = StorageExtensions.NoKey;
      Text = original.Text;
      onCloned(this);
    }
    partial void onCloned(LookupParentR clone);


    /// <summary>
    /// Constructor for LookupParentR read from CSV file
    /// </summary>
    private LookupParentR(int key, CsvReader csvReader){
      Key = key;
      Text = csvReader.ReadString();
      onCsvConstruct();
    }
    partial void onCsvConstruct();


    /// <summary>
    /// New LookupParentR read from CSV file
    /// </summary>
    internal static LookupParentR Create(int key, CsvReader csvReader) {
      return new LookupParentR(key, csvReader);
    }
    #endregion


    #region Methods
    //      -------

    /// <summary>
    /// Adds LookupParentR to DC.Data.LookupParentRs.<br/>
    /// Throws an Exception when LookupParentR is already stored.
    /// </summary>
    public void Store() {
      if (Key>=0) {
        throw new Exception($"LookupParentR cannot be stored again in DC.Data, key {Key} is greater equal 0." + Environment.NewLine + ToString());
      }

      var isCancelled = false;
      onStoring(ref isCancelled);
      if (isCancelled) return;

      DC.Data._LookupParentRs.Add(this);
      onStored();
#if DEBUG
      DC.Trace?.Invoke($"Stored LookupParentR #{GetHashCode()} @{Key}");
#endif
    }
    partial void onStoring(ref bool isCancelled);
    partial void onStored();


    /// <summary>
    /// Estimated number of UTF8 characters needed to write LookupParentR to CSV file
    /// </summary>
    public const int EstimatedLineLength = 150;


    /// <summary>
    /// Write LookupParentR to CSV file
    /// </summary>
    internal static void Write(LookupParentR lookupParentR, CsvWriter csvWriter) {
      lookupParentR.onCsvWrite();
      csvWriter.Write(lookupParentR.Text);
    }
    partial void onCsvWrite();


    /// <summary>
    /// Updates LookupParentR with the provided values
    /// </summary>
    public void Update(string text) {
      var clone = new LookupParentR(this);
      var isCancelled = false;
      onUpdating(text, ref isCancelled);
      if (isCancelled) return;

#if DEBUG
      DC.Trace?.Invoke($"Updating LookupParentR: {ToTraceString()}");
#endif

      //update properties and detect if any value has changed
      var isChangeDetected = false;
      if (Text!=text) {
        Text = text;
        isChangeDetected = true;
      }
      if (isChangeDetected) {
        onUpdated(clone);
        if (Key>=0) {
          DC.Data._LookupParentRs.ItemHasChanged(clone, this);
        } else if (DC.Data.IsTransaction) {
          DC.Data.AddTransaction(new TransactionItem(2, TransactionActivityEnum.Update, Key, this, oldItem: clone));
        }
        HasChanged?.Invoke(clone, this);
      }
#if DEBUG
      DC.Trace?.Invoke($"Updated LookupParentR: {ToTraceString()}");
#endif
    }
    partial void onUpdating(string text, ref bool isCancelled);
    partial void onUpdated(LookupParentR old);


    /// <summary>
    /// Updates this LookupParentR with values from CSV file
    /// </summary>
    internal static void Update(LookupParentR lookupParentR, CsvReader csvReader){
      lookupParentR.Text = csvReader.ReadString();
      lookupParentR.onCsvUpdate();
    }
    partial void onCsvUpdate();


    /// <summary>
    /// Releasing LookupParentR from DC.Data.LookupParentRs is not supported.
    /// </summary>
    public void Release() {
      throw new NotSupportedException("Release() is not supported, StorageClass attribute AreInstancesReleasable is false.");
    }


    /// <summary>
    /// Undoes the new() statement as part of a transaction rollback.
    /// </summary>
    internal static void RollbackItemNew(IStorageItem item) {
      var lookupParentR = (LookupParentR) item;
#if DEBUG
      DC.Trace?.Invoke($"Rollback new LookupParentR(): {lookupParentR.ToTraceString()}");
#endif
      lookupParentR.onRollbackItemNew();
    }
    partial void onRollbackItemNew();


    /// <summary>
    /// Releases LookupParentR from DC.Data.LookupParentRs as part of a transaction rollback of Store().
    /// </summary>
    internal static void RollbackItemStore(IStorageItem item) {
      var lookupParentR = (LookupParentR) item;
#if DEBUG
      DC.Trace?.Invoke($"Rollback LookupParentR.Store(): {lookupParentR.ToTraceString()}");
#endif
      lookupParentR.onRollbackItemStored();
    }
    partial void onRollbackItemStored();


    /// <summary>
    /// Restores the LookupParentR item data as it was before the last update as part of a transaction rollback.
    /// </summary>
    internal static void RollbackItemUpdate(IStorageItem oldStorageItem, IStorageItem newStorageItem) {
      var oldItem = (LookupParentR) oldStorageItem;//an item clone with the values before item was updated
      var item = (LookupParentR) newStorageItem;//is the instance whose values should be restored
#if DEBUG
      DC.Trace?.Invoke($"Rolling back LookupParentR.Update(): {item.ToTraceString()}");
#endif

      // updated item: restore old values
      item.Text = oldItem.Text;
      item.onRollbackItemUpdated(oldItem);
#if DEBUG
      DC.Trace?.Invoke($"Rolled back LookupParentR.Update(): {item.ToTraceString()}");
#endif
    }
    partial void onRollbackItemUpdated(LookupParentR oldLookupParentR);


    /// <summary>
    /// Adds LookupParentR to DC.Data.LookupParentRs as part of a transaction rollback of Release().
    /// </summary>
    internal static void RollbackItemRelease(IStorageItem item) {
      var lookupParentR = (LookupParentR) item;
#if DEBUG
      DC.Trace?.Invoke($"Rollback LookupParentR.Release(): {lookupParentR.ToTraceString()}");
#endif
      lookupParentR.onRollbackItemRelease();
    }
    partial void onRollbackItemRelease();


    /// <summary>
    /// Returns property values for tracing. Parents are shown with their key instead their content.
    /// </summary>
    public string ToTraceString() {
      var returnString =
        $"{this.GetKeyOrHash()}|" +
        $" {Text}";
      onToTraceString(ref returnString);
      return returnString;
    }
    partial void onToTraceString(ref string returnString);


    /// <summary>
    /// Returns property values
    /// </summary>
    public string ToShortString() {
      var returnString =
        $"{Key.ToKeyString()}," +
        $" {Text}";
      onToShortString(ref returnString);
      return returnString;
    }
    partial void onToShortString(ref string returnString);


    /// <summary>
    /// Returns all property names and values
    /// </summary>
    public override string ToString() {
      var returnString =
        $"Key: {Key.ToKeyString()}," +
        $" Text: {Text};";
      onToString(ref returnString);
      return returnString;
    }
    partial void onToString(ref string returnString);
    #endregion
  }
}
