//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by StorageClassGenerator
//
//     Do not change code in this file, it will get lost when the file gets 
//     auto generated again. Write your code into DictionaryParentNR.cs.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
using System;
using System.Collections.Generic;
using System.Threading;
using StorageLib;


namespace StorageDataContext  {


  public partial class DictionaryParentNR: IStorageItemGeneric<DictionaryParentNR> {

    #region Properties
    //      ----------

    /// <summary>
    /// Unique identifier for DictionaryParentNR. Gets set once DictionaryParentNR gets added to DC.Data.
    /// </summary>
    public int Key { get; private set; }
    internal static void SetKey(IStorageItem dictionaryParentNR, int key, bool isRollback) {
#if DEBUG
      if (isRollback) {
        if (key==StorageExtensions.NoKey) {
          DC.Trace?.Invoke($"Release DictionaryParentNR key @{dictionaryParentNR.Key} #{dictionaryParentNR.GetHashCode()}");
        } else {
          DC.Trace?.Invoke($"Store DictionaryParentNR key @{key} #{dictionaryParentNR.GetHashCode()}");
        }
      }
#endif
      ((DictionaryParentNR)dictionaryParentNR).Key = key;
    }


    public string Text { get; private set; }


    public IReadOnlyDictionary<string, DictionaryChild> DictionaryChidren => dictionaryChidren;
    readonly Dictionary<string, DictionaryChild> dictionaryChidren;


    /// <summary>
    /// Headers written to first line in CSV file
    /// </summary>
    internal static readonly string[] Headers = {"Key", "Text"};


    /// <summary>
    /// None existing DictionaryParentNR
    /// </summary>
    internal static DictionaryParentNR NoDictionaryParentNR = new DictionaryParentNR("NoText", isStoring: false);
    #endregion


    #region Events
    //      ------

    /// <summary>
    /// Content of DictionaryParentNR has changed. Gets only raised for changes occurring after loading DC.Data with previously stored data.
    /// </summary>
    public event Action</*old*/DictionaryParentNR, /*new*/DictionaryParentNR>? HasChanged;
    #endregion


    #region Constructors
    //      ------------

    /// <summary>
    /// DictionaryParentNR Constructor. If isStoring is true, adds DictionaryParentNR to DC.Data.DictionaryParentNRs.
    /// </summary>
    public DictionaryParentNR(string text, bool isStoring = true) {
      Key = StorageExtensions.NoKey;
      Text = text;
      dictionaryChidren = new Dictionary<string, DictionaryChild>();
#if DEBUG
      DC.Trace?.Invoke($"new DictionaryParentNR: {ToTraceString()}");
#endif
      onConstruct();
      if (DC.Data.IsTransaction) {
        DC.Data.AddTransaction(new TransactionItem(18,TransactionActivityEnum.New, Key, this));
      }

      if (isStoring) {
        Store();
      }
    }
    partial void onConstruct();


    /// <summary>
    /// Cloning constructor. It will copy all data from original except any collection (children).
    /// </summary>
    #pragma warning disable CS8618 // Children collections are uninitialized.
    public DictionaryParentNR(DictionaryParentNR original) {
    #pragma warning restore CS8618 //
      Key = StorageExtensions.NoKey;
      Text = original.Text;
      onCloned(this);
    }
    partial void onCloned(DictionaryParentNR clone);


    /// <summary>
    /// Constructor for DictionaryParentNR read from CSV file
    /// </summary>
    private DictionaryParentNR(int key, CsvReader csvReader){
      Key = key;
      Text = csvReader.ReadString();
      dictionaryChidren = new Dictionary<string, DictionaryChild>();
      onCsvConstruct();
    }
    partial void onCsvConstruct();


    /// <summary>
    /// New DictionaryParentNR read from CSV file
    /// </summary>
    internal static DictionaryParentNR Create(int key, CsvReader csvReader) {
      return new DictionaryParentNR(key, csvReader);
    }
    #endregion


    #region Methods
    //      -------

    /// <summary>
    /// Adds DictionaryParentNR to DC.Data.DictionaryParentNRs.<br/>
    /// Throws an Exception when DictionaryParentNR is already stored.
    /// </summary>
    public void Store() {
      if (Key>=0) {
        throw new Exception($"DictionaryParentNR cannot be stored again in DC.Data, key {Key} is greater equal 0." + Environment.NewLine + ToString());
      }

      var isCancelled = false;
      onStoring(ref isCancelled);
      if (isCancelled) return;

      DC.Data._DictionaryParentNRs.Add(this);
      onStored();
#if DEBUG
      DC.Trace?.Invoke($"Stored DictionaryParentNR #{GetHashCode()} @{Key}");
#endif
    }
    partial void onStoring(ref bool isCancelled);
    partial void onStored();


    /// <summary>
    /// Estimated number of UTF8 characters needed to write DictionaryParentNR to CSV file
    /// </summary>
    public const int EstimatedLineLength = 150;


    /// <summary>
    /// Write DictionaryParentNR to CSV file
    /// </summary>
    internal static void Write(DictionaryParentNR dictionaryParentNR, CsvWriter csvWriter) {
      dictionaryParentNR.onCsvWrite();
      csvWriter.Write(dictionaryParentNR.Text);
    }
    partial void onCsvWrite();


    /// <summary>
    /// Updates DictionaryParentNR with the provided values
    /// </summary>
    public void Update(string text) {
      var clone = new DictionaryParentNR(this);
      var isCancelled = false;
      onUpdating(text, ref isCancelled);
      if (isCancelled) return;

#if DEBUG
      DC.Trace?.Invoke($"Updating DictionaryParentNR: {ToTraceString()}");
#endif

      //update properties and detect if any value has changed
      var isChangeDetected = false;
      if (Text!=text) {
        Text = text;
        isChangeDetected = true;
      }
      if (isChangeDetected) {
        onUpdated(clone);
        if (Key>=0) {
          DC.Data._DictionaryParentNRs.ItemHasChanged(clone, this);
        } else if (DC.Data.IsTransaction) {
          DC.Data.AddTransaction(new TransactionItem(18, TransactionActivityEnum.Update, Key, this, oldItem: clone));
        }
        HasChanged?.Invoke(clone, this);
      }
#if DEBUG
      DC.Trace?.Invoke($"Updated DictionaryParentNR: {ToTraceString()}");
#endif
    }
    partial void onUpdating(string text, ref bool isCancelled);
    partial void onUpdated(DictionaryParentNR old);


    /// <summary>
    /// Updates this DictionaryParentNR with values from CSV file
    /// </summary>
    internal static void Update(DictionaryParentNR dictionaryParentNR, CsvReader csvReader){
      dictionaryParentNR.Text = csvReader.ReadString();
      dictionaryParentNR.onCsvUpdate();
    }
    partial void onCsvUpdate();


    /// <summary>
    /// Add dictionaryChild to DictionaryChidren.
    /// </summary>
    internal void AddToDictionaryChidren(DictionaryChild dictionaryChild) {
#if DEBUG
      if (dictionaryChild==DictionaryChild.NoDictionaryChild) throw new Exception();
      if ((dictionaryChild.Key>=0)&&(Key<0)) throw new Exception();
      if (dictionaryChidren.ContainsKey(dictionaryChild.Text)) throw new Exception();
#endif
      dictionaryChidren.Add(dictionaryChild.Text, dictionaryChild);
      onAddedToDictionaryChidren(dictionaryChild);
#if DEBUG
      DC.Trace?.Invoke($"Add DictionaryChild {dictionaryChild.GetKeyOrHash()} to " +
        $"{this.GetKeyOrHash()} DictionaryParentNR.DictionaryChidren");
#endif
    }
    partial void onAddedToDictionaryChidren(DictionaryChild dictionaryChild);


    /// <summary>
    /// Removes dictionaryChild from DictionaryParentNR.
    /// </summary>
    internal void RemoveFromDictionaryChidren(DictionaryChild dictionaryChild) {
#if DEBUG
      if (!dictionaryChidren.Remove(dictionaryChild.Text)) throw new Exception();
#else
        dictionaryChidren.Remove(dictionaryChild.Text);
#endif
      onRemovedFromDictionaryChidren(dictionaryChild);
#if DEBUG
      DC.Trace?.Invoke($"Remove DictionaryChild {dictionaryChild.GetKeyOrHash()} from " +
        $"{this.GetKeyOrHash()} DictionaryParentNR.DictionaryChidren");
#endif
    }
    partial void onRemovedFromDictionaryChidren(DictionaryChild dictionaryChild);


    /// <summary>
    /// Removes DictionaryParentNR from DC.Data.DictionaryParentNRs.
    /// </summary>
    public void Release() {
      if (Key<0) {
        throw new Exception($"DictionaryParentNR.Release(): DictionaryParentNR '{this}' is not stored in DC.Data, key is {Key}.");
      }
      foreach (var dictionaryChild in DictionaryChidren.Values) {
        if (dictionaryChild?.Key>=0) {
          throw new Exception($"Cannot release DictionaryParentNR '{this}' " + Environment.NewLine + 
            $"because '{dictionaryChild}' in DictionaryParentNR.DictionaryChidren is still stored.");
        }
      }
      onReleased();
      DC.Data._DictionaryParentNRs.Remove(Key);
#if DEBUG
      DC.Trace?.Invoke($"Released DictionaryParentNR @{Key} #{GetHashCode()}");
#endif
    }
    partial void onReleased();


    /// <summary>
    /// Undoes the new() statement as part of a transaction rollback.
    /// </summary>
    internal static void RollbackItemNew(IStorageItem item) {
      var dictionaryParentNR = (DictionaryParentNR) item;
#if DEBUG
      DC.Trace?.Invoke($"Rollback new DictionaryParentNR(): {dictionaryParentNR.ToTraceString()}");
#endif
      dictionaryParentNR.onRollbackItemNew();
    }
    partial void onRollbackItemNew();


    /// <summary>
    /// Releases DictionaryParentNR from DC.Data.DictionaryParentNRs as part of a transaction rollback of Store().
    /// </summary>
    internal static void RollbackItemStore(IStorageItem item) {
      var dictionaryParentNR = (DictionaryParentNR) item;
#if DEBUG
      DC.Trace?.Invoke($"Rollback DictionaryParentNR.Store(): {dictionaryParentNR.ToTraceString()}");
#endif
      dictionaryParentNR.onRollbackItemStored();
    }
    partial void onRollbackItemStored();


    /// <summary>
    /// Restores the DictionaryParentNR item data as it was before the last update as part of a transaction rollback.
    /// </summary>
    internal static void RollbackItemUpdate(IStorageItem oldStorageItem, IStorageItem newStorageItem) {
      var oldItem = (DictionaryParentNR) oldStorageItem;//an item clone with the values before item was updated
      var item = (DictionaryParentNR) newStorageItem;//is the instance whose values should be restored
#if DEBUG
      DC.Trace?.Invoke($"Rolling back DictionaryParentNR.Update(): {item.ToTraceString()}");
#endif

      // updated item: restore old values
      item.Text = oldItem.Text;
      item.onRollbackItemUpdated(oldItem);
#if DEBUG
      DC.Trace?.Invoke($"Rolled back DictionaryParentNR.Update(): {item.ToTraceString()}");
#endif
    }
    partial void onRollbackItemUpdated(DictionaryParentNR oldDictionaryParentNR);


    /// <summary>
    /// Adds DictionaryParentNR to DC.Data.DictionaryParentNRs as part of a transaction rollback of Release().
    /// </summary>
    internal static void RollbackItemRelease(IStorageItem item) {
      var dictionaryParentNR = (DictionaryParentNR) item;
#if DEBUG
      DC.Trace?.Invoke($"Rollback DictionaryParentNR.Release(): {dictionaryParentNR.ToTraceString()}");
#endif
      dictionaryParentNR.onRollbackItemRelease();
    }
    partial void onRollbackItemRelease();


    /// <summary>
    /// Returns property values for tracing. Parents are shown with their key instead their content.
    /// </summary>
    public string ToTraceString() {
      var returnString =
        $"{this.GetKeyOrHash()}|" +
        $" {Text}";
      onToTraceString(ref returnString);
      return returnString;
    }
    partial void onToTraceString(ref string returnString);


    /// <summary>
    /// Returns property values
    /// </summary>
    public string ToShortString() {
      var returnString =
        $"{Key.ToKeyString()}," +
        $" {Text}";
      onToShortString(ref returnString);
      return returnString;
    }
    partial void onToShortString(ref string returnString);


    /// <summary>
    /// Returns all property names and values
    /// </summary>
    public override string ToString() {
      var returnString =
        $"Key: {Key.ToKeyString()}," +
        $" Text: {Text}," +
        $" DictionaryChidren: {DictionaryChidren.Count};";
      onToString(ref returnString);
      return returnString;
    }
    partial void onToString(ref string returnString);
    #endregion
  }
}
