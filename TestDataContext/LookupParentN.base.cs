//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by StorageClassGenerator
//
//     Do not change code in this file, it will get lost when the file gets 
//     auto generated again. Write your code into LookupParentN.cs.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
using System;
using System.Collections.Generic;
using System.Threading;
using StorageLib;


namespace TestContext  {


  public partial class LookupParentN: IStorageItemGeneric<LookupParentN> {

    #region Properties
    //      ----------

    /// <summary>
    /// Unique identifier for LookupParentN. Gets set once LookupParentN gets added to DC.Data.
    /// </summary>
    public int Key { get; private set; }
    internal static void SetKey(IStorageItem lookupParentN, int key, bool isRollback) {
#if DEBUG
      if (isRollback) {
        if (key==StorageExtensions.NoKey) {
          DC.Trace?.Invoke($"Release LookupParentN key @{lookupParentN.Key} #{lookupParentN.GetHashCode()}");
        } else {
          DC.Trace?.Invoke($"Store LookupParentN key @{key} #{lookupParentN.GetHashCode()}");
        }
      }
#endif
      ((LookupParentN)lookupParentN).Key = key;
    }


    public string Text { get; private set; }


    /// <summary>
    /// Headers written to first line in CSV file
    /// </summary>
    internal static readonly string[] Headers = {"Key", "Text"};


    /// <summary>
    /// None existing LookupParentN, used as a temporary place holder when reading a CSV file
    /// which was not compacted. It might create first a later deleted item linking to a 
    /// deleted parent. In this case, the parent property gets set to NoLookupParentN. Once the CSV
    /// file is completely read, that child will actually be deleted (released) and Verify()
    /// ensures that there are no stored children with links to NoLookupParentN.
    /// </summary>
    internal static LookupParentN NoLookupParentN = new LookupParentN("NoText", isStoring: false);
    #endregion


    #region Events
    //      ------

    /// <summary>
    /// Content of LookupParentN has changed. Gets only raised for changes occurring after loading DC.Data with previously stored data.
    /// </summary>
    public event Action</*old*/LookupParentN, /*new*/LookupParentN>? HasChanged;
    #endregion


    #region Constructors
    //      ------------

    /// <summary>
    /// LookupParentN Constructor. If isStoring is true, adds LookupParentN to DC.Data.LookupParentNs.
    /// </summary>
    public LookupParentN(string text, bool isStoring = true) {
      Key = StorageExtensions.NoKey;
      Text = text;
#if DEBUG
      DC.Trace?.Invoke($"new LookupParentN: {ToTraceString()}");
#endif
      onConstruct();
      if (DC.Data.IsTransaction) {
        DC.Data.AddTransaction(new TransactionItem(1,TransactionActivityEnum.New, Key, this));
      }

      if (isStoring) {
        Store();
      }
    }
    partial void onConstruct();


    /// <summary>
    /// Cloning constructor. It will copy all data from original except any collection (children).
    /// </summary>
    #pragma warning disable CS8618 // Children collections are uninitialized.
    public LookupParentN(LookupParentN original) {
    #pragma warning restore CS8618 //
      Key = StorageExtensions.NoKey;
      Text = original.Text;
      onCloned(this);
    }
    partial void onCloned(LookupParentN clone);


    /// <summary>
    /// Constructor for LookupParentN read from CSV file
    /// </summary>
    private LookupParentN(int key, CsvReader csvReader){
      Key = key;
      Text = csvReader.ReadString();
      onCsvConstruct();
    }
    partial void onCsvConstruct();


    /// <summary>
    /// New LookupParentN read from CSV file
    /// </summary>
    internal static LookupParentN Create(int key, CsvReader csvReader) {
      return new LookupParentN(key, csvReader);
    }
    #endregion


    #region Methods
    //      -------

    /// <summary>
    /// Adds LookupParentN to DC.Data.LookupParentNs.<br/>
    /// Throws an Exception when LookupParentN is already stored.
    /// </summary>
    public void Store() {
      if (Key>=0) {
        throw new Exception($"LookupParentN cannot be stored again in DC.Data, key {Key} is greater equal 0." + Environment.NewLine + ToString());
      }

      var isCancelled = false;
      onStoring(ref isCancelled);
      if (isCancelled) return;

      DC.Data._LookupParentNs.Add(this);
      onStored();
#if DEBUG
      DC.Trace?.Invoke($"Stored LookupParentN #{GetHashCode()} @{Key}");
#endif
    }
    partial void onStoring(ref bool isCancelled);
    partial void onStored();


    /// <summary>
    /// Estimated number of UTF8 characters needed to write LookupParentN to CSV file
    /// </summary>
    public const int EstimatedLineLength = 150;


    /// <summary>
    /// Write LookupParentN to CSV file
    /// </summary>
    internal static void Write(LookupParentN lookupParentN, CsvWriter csvWriter) {
      lookupParentN.onCsvWrite();
      csvWriter.Write(lookupParentN.Text);
    }
    partial void onCsvWrite();


    /// <summary>
    /// Updates LookupParentN with the provided values
    /// </summary>
    public void Update(string text) {
      var clone = new LookupParentN(this);
      var isCancelled = false;
      onUpdating(text, ref isCancelled);
      if (isCancelled) return;

#if DEBUG
      DC.Trace?.Invoke($"Updating LookupParentN: {ToTraceString()}");
#endif

      //update properties and detect if any value has changed
      var isChangeDetected = false;
      if (Text!=text) {
        Text = text;
        isChangeDetected = true;
      }
      if (isChangeDetected) {
        onUpdated(clone);
        if (Key>=0) {
          DC.Data._LookupParentNs.ItemHasChanged(clone, this);
        } else if (DC.Data.IsTransaction) {
          DC.Data.AddTransaction(new TransactionItem(1, TransactionActivityEnum.Update, Key, this, oldItem: clone));
        }
        HasChanged?.Invoke(clone, this);
      }
#if DEBUG
      DC.Trace?.Invoke($"Updated LookupParentN: {ToTraceString()}");
#endif
    }
    partial void onUpdating(string text, ref bool isCancelled);
    partial void onUpdated(LookupParentN old);


    /// <summary>
    /// Updates this LookupParentN with values from CSV file
    /// </summary>
    internal static void Update(LookupParentN lookupParentN, CsvReader csvReader){
      lookupParentN.Text = csvReader.ReadString();
      lookupParentN.onCsvUpdate();
    }
    partial void onCsvUpdate();


    /// <summary>
    /// Releasing LookupParentN from DC.Data.LookupParentNs is not supported.
    /// </summary>
    public void Release() {
      throw new NotSupportedException("Release() is not supported, StorageClass attribute AreInstancesReleasable is false.");
    }


    /// <summary>
    /// Undoes the new() statement as part of a transaction rollback.
    /// </summary>
    internal static void RollbackItemNew(IStorageItem item) {
      var lookupParentN = (LookupParentN) item;
#if DEBUG
      DC.Trace?.Invoke($"Rollback new LookupParentN(): {lookupParentN.ToTraceString()}");
#endif
      lookupParentN.onRollbackItemNew();
    }
    partial void onRollbackItemNew();


    /// <summary>
    /// Releases LookupParentN from DC.Data.LookupParentNs as part of a transaction rollback of Store().
    /// </summary>
    internal static void RollbackItemStore(IStorageItem item) {
      var lookupParentN = (LookupParentN) item;
#if DEBUG
      DC.Trace?.Invoke($"Rollback LookupParentN.Store(): {lookupParentN.ToTraceString()}");
#endif
      lookupParentN.onRollbackItemStored();
    }
    partial void onRollbackItemStored();


    /// <summary>
    /// Restores the LookupParentN item data as it was before the last update as part of a transaction rollback.
    /// </summary>
    internal static void RollbackItemUpdate(IStorageItem oldStorageItem, IStorageItem newStorageItem) {
      var oldItem = (LookupParentN) oldStorageItem;//an item clone with the values before item was updated
      var item = (LookupParentN) newStorageItem;//is the instance whose values should be restored
#if DEBUG
      DC.Trace?.Invoke($"Rolling back LookupParentN.Update(): {item.ToTraceString()}");
#endif

      // updated item: restore old values
      item.Text = oldItem.Text;
      item.onRollbackItemUpdated(oldItem);
#if DEBUG
      DC.Trace?.Invoke($"Rolled back LookupParentN.Update(): {item.ToTraceString()}");
#endif
    }
    partial void onRollbackItemUpdated(LookupParentN oldLookupParentN);


    /// <summary>
    /// Adds LookupParentN to DC.Data.LookupParentNs as part of a transaction rollback of Release().
    /// </summary>
    internal static void RollbackItemRelease(IStorageItem item) {
      var lookupParentN = (LookupParentN) item;
#if DEBUG
      DC.Trace?.Invoke($"Rollback LookupParentN.Release(): {lookupParentN.ToTraceString()}");
#endif
      lookupParentN.onRollbackItemRelease();
    }
    partial void onRollbackItemRelease();


    /// <summary>
    /// Returns property values for tracing. Parents are shown with their key instead their content.
    /// </summary>
    public string ToTraceString() {
      var returnString =
        $"{this.GetKeyOrHash()}|" +
        $" {Text}";
      onToTraceString(ref returnString);
      return returnString;
    }
    partial void onToTraceString(ref string returnString);


    /// <summary>
    /// Returns property values
    /// </summary>
    public string ToShortString() {
      var returnString =
        $"{Key.ToKeyString()}," +
        $" {Text}";
      onToShortString(ref returnString);
      return returnString;
    }
    partial void onToShortString(ref string returnString);


    /// <summary>
    /// Returns all property names and values
    /// </summary>
    public override string ToString() {
      var returnString =
        $"Key: {Key.ToKeyString()}," +
        $" Text: {Text};";
      onToString(ref returnString);
      return returnString;
    }
    partial void onToString(ref string returnString);
    #endregion
  }
}
