//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by StorageClassGenerator
//
//     Do not change code in this file, it will get lost when the file gets 
//     auto generated again. Write your code into SortedListParentR.cs.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
using System;
using System.Collections.Generic;
using System.Threading;
using StorageLib;


namespace TestContext  {


  public partial class SortedListParentR: IStorageItemGeneric<SortedListParentR> {

    #region Properties
    //      ----------

    /// <summary>
    /// Unique identifier for SortedListParentR. Gets set once SortedListParentR gets added to DC.Data.
    /// </summary>
    public int Key { get; private set; }
    internal static void SetKey(IStorageItem sortedListParentR, int key, bool isRollback) {
#if DEBUG
      if (isRollback) {
        if (key==StorageExtensions.NoKey) {
          DC.Trace?.Invoke($"Release SortedListParentR key @{sortedListParentR.Key} #{sortedListParentR.GetHashCode()}");
        } else {
          DC.Trace?.Invoke($"Store SortedListParentR key @{key} #{sortedListParentR.GetHashCode()}");
        }
      }
#endif
      ((SortedListParentR)sortedListParentR).Key = key;
    }


    public string Text { get; private set; }


    public IReadOnlyDictionary<string, SortedListChild> SortedListChidren => sortedListChidren;
    readonly SortedList<string, SortedListChild> sortedListChidren;


    /// <summary>
    /// Headers written to first line in CSV file
    /// </summary>
    internal static readonly string[] Headers = {"Key", "Text"};


    /// <summary>
    /// None existing SortedListParentR, used as a temporary place holder when reading a CSV file
    /// which was not compacted. It might create first a later deleted item linking to a 
    /// deleted parent. In this case, the parent property gets set to NoSortedListParentR. Once the CSV
    /// file is completely read, that child will actually be deleted (released) and Verify()
    /// ensures that there are no stored children with links to NoSortedListParentR.
    /// </summary>
    internal static SortedListParentR NoSortedListParentR = new SortedListParentR("NoText", isStoring: false);
    #endregion


    #region Events
    //      ------

    /// <summary>
    /// Content of SortedListParentR has changed. Gets only raised for changes occurring after loading DC.Data with previously stored data.
    /// </summary>
    public event Action</*old*/SortedListParentR, /*new*/SortedListParentR>? HasChanged;
    #endregion


    #region Constructors
    //      ------------

    /// <summary>
    /// SortedListParentR Constructor. If isStoring is true, adds SortedListParentR to DC.Data.SortedListParentRs.
    /// </summary>
    public SortedListParentR(string text, bool isStoring = true) {
      Key = StorageExtensions.NoKey;
      Text = text;
      sortedListChidren = new SortedList<string, SortedListChild>();
#if DEBUG
      DC.Trace?.Invoke($"new SortedListParentR: {ToTraceString()}");
#endif
      onConstruct();
      if (DC.Data.IsTransaction) {
        DC.Data.AddTransaction(new TransactionItem(22,TransactionActivityEnum.New, Key, this));
      }

      if (isStoring) {
        Store();
      }
    }
    partial void onConstruct();


    /// <summary>
    /// Cloning constructor. It will copy all data from original except any collection (children).
    /// </summary>
    #pragma warning disable CS8618 // Children collections are uninitialized.
    public SortedListParentR(SortedListParentR original) {
    #pragma warning restore CS8618 //
      Key = StorageExtensions.NoKey;
      Text = original.Text;
      onCloned(this);
    }
    partial void onCloned(SortedListParentR clone);


    /// <summary>
    /// Constructor for SortedListParentR read from CSV file
    /// </summary>
    private SortedListParentR(int key, CsvReader csvReader){
      Key = key;
      Text = csvReader.ReadString();
      sortedListChidren = new SortedList<string, SortedListChild>();
      onCsvConstruct();
    }
    partial void onCsvConstruct();


    /// <summary>
    /// New SortedListParentR read from CSV file
    /// </summary>
    internal static SortedListParentR Create(int key, CsvReader csvReader) {
      return new SortedListParentR(key, csvReader);
    }
    #endregion


    #region Methods
    //      -------

    /// <summary>
    /// Adds SortedListParentR to DC.Data.SortedListParentRs.<br/>
    /// Throws an Exception when SortedListParentR is already stored.
    /// </summary>
    public void Store() {
      if (Key>=0) {
        throw new Exception($"SortedListParentR cannot be stored again in DC.Data, key {Key} is greater equal 0." + Environment.NewLine + ToString());
      }

      var isCancelled = false;
      onStoring(ref isCancelled);
      if (isCancelled) return;

      DC.Data._SortedListParentRs.Add(this);
      onStored();
#if DEBUG
      DC.Trace?.Invoke($"Stored SortedListParentR #{GetHashCode()} @{Key}");
#endif
    }
    partial void onStoring(ref bool isCancelled);
    partial void onStored();


    /// <summary>
    /// Estimated number of UTF8 characters needed to write SortedListParentR to CSV file
    /// </summary>
    public const int EstimatedLineLength = 150;


    /// <summary>
    /// Write SortedListParentR to CSV file
    /// </summary>
    internal static void Write(SortedListParentR sortedListParentR, CsvWriter csvWriter) {
      sortedListParentR.onCsvWrite();
      csvWriter.Write(sortedListParentR.Text);
    }
    partial void onCsvWrite();


    /// <summary>
    /// Updates SortedListParentR with the provided values
    /// </summary>
    public void Update(string text) {
      var clone = new SortedListParentR(this);
      var isCancelled = false;
      onUpdating(text, ref isCancelled);
      if (isCancelled) return;

#if DEBUG
      DC.Trace?.Invoke($"Updating SortedListParentR: {ToTraceString()}");
#endif

      //update properties and detect if any value has changed
      var isChangeDetected = false;
      if (Text!=text) {
        Text = text;
        isChangeDetected = true;
      }
      if (isChangeDetected) {
        onUpdated(clone);
        if (Key>=0) {
          DC.Data._SortedListParentRs.ItemHasChanged(clone, this);
        } else if (DC.Data.IsTransaction) {
          DC.Data.AddTransaction(new TransactionItem(22, TransactionActivityEnum.Update, Key, this, oldItem: clone));
        }
        HasChanged?.Invoke(clone, this);
      }
#if DEBUG
      DC.Trace?.Invoke($"Updated SortedListParentR: {ToTraceString()}");
#endif
    }
    partial void onUpdating(string text, ref bool isCancelled);
    partial void onUpdated(SortedListParentR old);


    /// <summary>
    /// Updates this SortedListParentR with values from CSV file
    /// </summary>
    internal static void Update(SortedListParentR sortedListParentR, CsvReader csvReader){
      sortedListParentR.Text = csvReader.ReadString();
      sortedListParentR.onCsvUpdate();
    }
    partial void onCsvUpdate();


    /// <summary>
    /// Add sortedListChild to SortedListChidren.
    /// </summary>
    internal void AddToSortedListChidren(SortedListChild sortedListChild) {
#if DEBUG
      if (sortedListChild==SortedListChild.NoSortedListChild) throw new Exception();
      if ((sortedListChild.Key>=0)&&(Key<0)) throw new Exception();
      if (sortedListChidren.ContainsKey(sortedListChild.Text)) throw new Exception();
#endif
      sortedListChidren.Add(sortedListChild.Text, sortedListChild);
      onAddedToSortedListChidren(sortedListChild);
#if DEBUG
      DC.Trace?.Invoke($"Add SortedListChild {sortedListChild.GetKeyOrHash()} to " +
        $"{this.GetKeyOrHash()} SortedListParentR.SortedListChidren");
#endif
    }
    partial void onAddedToSortedListChidren(SortedListChild sortedListChild);


    /// <summary>
    /// Removes sortedListChild from SortedListParentR.
    /// </summary>
    internal void RemoveFromSortedListChidren(SortedListChild sortedListChild) {
#if DEBUG
      if (!sortedListChidren.Remove(sortedListChild.Text)) throw new Exception();
#else
        sortedListChidren.Remove(sortedListChild.Text);
#endif
      onRemovedFromSortedListChidren(sortedListChild);
#if DEBUG
      DC.Trace?.Invoke($"Remove SortedListChild {sortedListChild.GetKeyOrHash()} from " +
        $"{this.GetKeyOrHash()} SortedListParentR.SortedListChidren");
#endif
    }
    partial void onRemovedFromSortedListChidren(SortedListChild sortedListChild);


    /// <summary>
    /// Removes SortedListParentR from DC.Data.SortedListParentRs.
    /// </summary>
    public void Release() {
      if (Key<0) {
        throw new Exception($"SortedListParentR.Release(): SortedListParentR '{this}' is not stored in DC.Data, key is {Key}.");
      }
      foreach (var sortedListChild in SortedListChidren.Values) {
        if (sortedListChild?.Key>=0) {
          throw new Exception($"Cannot release SortedListParentR '{this}' " + Environment.NewLine + 
            $"because '{sortedListChild}' in SortedListParentR.SortedListChidren is still stored.");
        }
      }
      onReleased();
      DC.Data._SortedListParentRs.Remove(Key);
#if DEBUG
      DC.Trace?.Invoke($"Released SortedListParentR @{Key} #{GetHashCode()}");
#endif
    }
    partial void onReleased();


    /// <summary>
    /// Undoes the new() statement as part of a transaction rollback.
    /// </summary>
    internal static void RollbackItemNew(IStorageItem item) {
      var sortedListParentR = (SortedListParentR) item;
#if DEBUG
      DC.Trace?.Invoke($"Rollback new SortedListParentR(): {sortedListParentR.ToTraceString()}");
#endif
      sortedListParentR.onRollbackItemNew();
    }
    partial void onRollbackItemNew();


    /// <summary>
    /// Releases SortedListParentR from DC.Data.SortedListParentRs as part of a transaction rollback of Store().
    /// </summary>
    internal static void RollbackItemStore(IStorageItem item) {
      var sortedListParentR = (SortedListParentR) item;
#if DEBUG
      DC.Trace?.Invoke($"Rollback SortedListParentR.Store(): {sortedListParentR.ToTraceString()}");
#endif
      sortedListParentR.onRollbackItemStored();
    }
    partial void onRollbackItemStored();


    /// <summary>
    /// Restores the SortedListParentR item data as it was before the last update as part of a transaction rollback.
    /// </summary>
    internal static void RollbackItemUpdate(IStorageItem oldStorageItem, IStorageItem newStorageItem) {
      var oldItem = (SortedListParentR) oldStorageItem;//an item clone with the values before item was updated
      var item = (SortedListParentR) newStorageItem;//is the instance whose values should be restored
#if DEBUG
      DC.Trace?.Invoke($"Rolling back SortedListParentR.Update(): {item.ToTraceString()}");
#endif

      // updated item: restore old values
      item.Text = oldItem.Text;
      item.onRollbackItemUpdated(oldItem);
#if DEBUG
      DC.Trace?.Invoke($"Rolled back SortedListParentR.Update(): {item.ToTraceString()}");
#endif
    }
    partial void onRollbackItemUpdated(SortedListParentR oldSortedListParentR);


    /// <summary>
    /// Adds SortedListParentR to DC.Data.SortedListParentRs as part of a transaction rollback of Release().
    /// </summary>
    internal static void RollbackItemRelease(IStorageItem item) {
      var sortedListParentR = (SortedListParentR) item;
#if DEBUG
      DC.Trace?.Invoke($"Rollback SortedListParentR.Release(): {sortedListParentR.ToTraceString()}");
#endif
      sortedListParentR.onRollbackItemRelease();
    }
    partial void onRollbackItemRelease();


    /// <summary>
    /// Returns property values for tracing. Parents are shown with their key instead their content.
    /// </summary>
    public string ToTraceString() {
      var returnString =
        $"{this.GetKeyOrHash()}|" +
        $" {Text}";
      onToTraceString(ref returnString);
      return returnString;
    }
    partial void onToTraceString(ref string returnString);


    /// <summary>
    /// Returns property values
    /// </summary>
    public string ToShortString() {
      var returnString =
        $"{Key.ToKeyString()}," +
        $" {Text}";
      onToShortString(ref returnString);
      return returnString;
    }
    partial void onToShortString(ref string returnString);


    /// <summary>
    /// Returns all property names and values
    /// </summary>
    public override string ToString() {
      var returnString =
        $"Key: {Key.ToKeyString()}," +
        $" Text: {Text}," +
        $" SortedListChidren: {SortedListChidren.Count};";
      onToString(ref returnString);
      return returnString;
    }
    partial void onToString(ref string returnString);
    #endregion
  }
}
