//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by StorageClassGenerator
//
//     Do not change code in this file, it will get lost when the file gets 
//     auto generated again. Write your code into NeedsDictionaryClass.cs.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
using System;
using System.Collections.Generic;
using System.Threading;
using StorageLib;


namespace DataModelSamples  {


  public partial class NeedsDictionaryClass: IStorageItemGeneric<NeedsDictionaryClass> {

    #region Properties
    //      ----------

    /// <summary>
    /// Unique identifier for NeedsDictionaryClass. Gets set once NeedsDictionaryClass gets added to DC.Data.
    /// </summary>
    public int Key { get; private set; }
    internal static void SetKey(IStorageItem needsDictionaryClass, int key, bool _) {
      ((NeedsDictionaryClass)needsDictionaryClass).Key = key;
    }


    public string Name { get; private set; }


    public string Address { get; private set; }


    /// <summary>
    /// Headers written to first line in CSV file
    /// </summary>
    internal static readonly string[] Headers = {"Key", "Name", "Address"};


    /// <summary>
    /// None existing NeedsDictionaryClass, used as a temporary place holder when reading a CSV file
    /// which was not compacted. It might create first a later deleted item linking to a 
    /// deleted parent. In this case, the parent property gets set to NoNeedsDictionaryClass. Once the CSV
    /// file is completely read, that child will actually be deleted (released) and Verify()
    /// ensures that there are no stored children with links to NoNeedsDictionaryClass.
    /// </summary>
    internal static NeedsDictionaryClass NoNeedsDictionaryClass = new NeedsDictionaryClass("NoName", "NoAddress", isStoring: false);
    #endregion


    #region Events
    //      ------

    /// <summary>
    /// Content of NeedsDictionaryClass has changed. Gets only raised for changes occurring after loading DC.Data with previously stored data.
    /// </summary>
    public event Action</*old*/NeedsDictionaryClass, /*new*/NeedsDictionaryClass>? HasChanged;
    #endregion


    #region Constructors
    //      ------------

    /// <summary>
    /// NeedsDictionaryClass Constructor. If isStoring is true, adds NeedsDictionaryClass to DC.Data.NeedsDictionaryClasss.
    /// </summary>
    public NeedsDictionaryClass(string name, string address, bool isStoring = true) {
      Key = StorageExtensions.NoKey;
      Name = name;
      Address = address;
      onConstruct();
      if (DC.Data.IsTransaction) {
        DC.Data.AddTransaction(new TransactionItem(37,TransactionActivityEnum.New, Key, this));
      }

      if (isStoring) {
        Store();
      }
    }
    partial void onConstruct();


    /// <summary>
    /// Cloning constructor. It will copy all data from original except any collection (children).
    /// </summary>
    #pragma warning disable CS8618 // Children collections are uninitialized.
    public NeedsDictionaryClass(NeedsDictionaryClass original) {
    #pragma warning restore CS8618 //
      Key = StorageExtensions.NoKey;
      Name = original.Name;
      Address = original.Address;
      onCloned(this);
    }
    partial void onCloned(NeedsDictionaryClass clone);


    /// <summary>
    /// Constructor for NeedsDictionaryClass read from CSV file
    /// </summary>
    private NeedsDictionaryClass(int key, CsvReader csvReader){
      Key = key;
      Name = csvReader.ReadString();
      DC.Data._NeedsDictionaryClasssByName.Add(Name, this);
      Address = csvReader.ReadString();
      onCsvConstruct();
    }
    partial void onCsvConstruct();


    /// <summary>
    /// New NeedsDictionaryClass read from CSV file
    /// </summary>
    internal static NeedsDictionaryClass Create(int key, CsvReader csvReader) {
      return new NeedsDictionaryClass(key, csvReader);
    }
    #endregion


    #region Methods
    //      -------

    /// <summary>
    /// Adds NeedsDictionaryClass to DC.Data.NeedsDictionaryClasss.<br/>
    /// Throws an Exception when NeedsDictionaryClass is already stored.
    /// </summary>
    public void Store() {
      if (Key>=0) {
        throw new Exception($"NeedsDictionaryClass cannot be stored again in DC.Data, key {Key} is greater equal 0." + Environment.NewLine + ToString());
      }

      var isCancelled = false;
      onStoring(ref isCancelled);
      if (isCancelled) return;

      DC.Data._NeedsDictionaryClasssByName.Add(Name, this);
      DC.Data._NeedsDictionaryClasss.Add(this);
      onStored();
    }
    partial void onStoring(ref bool isCancelled);
    partial void onStored();


    /// <summary>
    /// Estimated number of UTF8 characters needed to write NeedsDictionaryClass to CSV file
    /// </summary>
    public const int EstimatedLineLength = 300;


    /// <summary>
    /// Write NeedsDictionaryClass to CSV file
    /// </summary>
    internal static void Write(NeedsDictionaryClass needsDictionaryClass, CsvWriter csvWriter) {
      needsDictionaryClass.onCsvWrite();
      csvWriter.Write(needsDictionaryClass.Name);
      csvWriter.Write(needsDictionaryClass.Address);
    }
    partial void onCsvWrite();


    /// <summary>
    /// Updates NeedsDictionaryClass with the provided values
    /// </summary>
    public void Update(string name, string address) {
      var clone = new NeedsDictionaryClass(this);
      var isCancelled = false;
      onUpdating(name, address, ref isCancelled);
      if (isCancelled) return;


      //update properties and detect if any value has changed
      var isChangeDetected = false;
      if (Name!=name) {
        if (Key>=0) {
            DC.Data._NeedsDictionaryClasssByName.Remove(Name);
        }
        Name = name;
        if (Key>=0) {
            DC.Data._NeedsDictionaryClasssByName.Add(Name, this);
        }
        isChangeDetected = true;
      }
      if (Address!=address) {
        Address = address;
        isChangeDetected = true;
      }
      if (isChangeDetected) {
        onUpdated(clone);
        if (Key>=0) {
          DC.Data._NeedsDictionaryClasss.ItemHasChanged(clone, this);
        } else if (DC.Data.IsTransaction) {
          DC.Data.AddTransaction(new TransactionItem(37, TransactionActivityEnum.Update, Key, this, oldItem: clone));
        }
        HasChanged?.Invoke(clone, this);
      }
    }
    partial void onUpdating(string name, string address, ref bool isCancelled);
    partial void onUpdated(NeedsDictionaryClass old);


    /// <summary>
    /// Updates this NeedsDictionaryClass with values from CSV file
    /// </summary>
    internal static void Update(NeedsDictionaryClass needsDictionaryClass, CsvReader csvReader){
      DC.Data._NeedsDictionaryClasssByName.Remove(needsDictionaryClass.Name);
      needsDictionaryClass.Name = csvReader.ReadString();
      DC.Data._NeedsDictionaryClasssByName.Add(needsDictionaryClass.Name, needsDictionaryClass);
      needsDictionaryClass.Address = csvReader.ReadString();
      needsDictionaryClass.onCsvUpdate();
    }
    partial void onCsvUpdate();


    /// <summary>
    /// Removes NeedsDictionaryClass from DC.Data.NeedsDictionaryClasss.
    /// </summary>
    public void Release() {
      if (Key<0) {
        throw new Exception($"NeedsDictionaryClass.Release(): NeedsDictionaryClass '{this}' is not stored in DC.Data, key is {Key}.");
      }
      DC.Data._NeedsDictionaryClasssByName.Remove(Name);
      DC.Data._NeedsDictionaryClasss.Remove(Key);
      onReleased();
    }
    partial void onReleased();


    /// <summary>
    /// Undoes the new() statement as part of a transaction rollback.
    /// </summary>
    internal static void RollbackItemNew(IStorageItem item) {
      var needsDictionaryClass = (NeedsDictionaryClass) item;
      needsDictionaryClass.onRollbackItemNew();
    }
    partial void onRollbackItemNew();


    /// <summary>
    /// Releases NeedsDictionaryClass from DC.Data.NeedsDictionaryClasss as part of a transaction rollback of Store().
    /// </summary>
    internal static void RollbackItemStore(IStorageItem item) {
      var needsDictionaryClass = (NeedsDictionaryClass) item;
      DC.Data._NeedsDictionaryClasssByName.Remove(needsDictionaryClass.Name);
      needsDictionaryClass.onRollbackItemStored();
    }
    partial void onRollbackItemStored();


    /// <summary>
    /// Restores the NeedsDictionaryClass item data as it was before the last update as part of a transaction rollback.
    /// </summary>
    internal static void RollbackItemUpdate(IStorageItem oldStorageItem, IStorageItem newStorageItem) {
      var oldItem = (NeedsDictionaryClass) oldStorageItem;//an item clone with the values before item was updated
      var item = (NeedsDictionaryClass) newStorageItem;//is the instance whose values should be restored

      // remove updated item from dictionaries
      DC.Data._NeedsDictionaryClasssByName.Remove(item.Name);

      // updated item: restore old values
      item.Name = oldItem.Name;
      item.Address = oldItem.Address;

      // add item with previous values to dictionaries
      DC.Data._NeedsDictionaryClasssByName.Add(item.Name, item);
      item.onRollbackItemUpdated(oldItem);
    }
    partial void onRollbackItemUpdated(NeedsDictionaryClass oldNeedsDictionaryClass);


    /// <summary>
    /// Adds NeedsDictionaryClass to DC.Data.NeedsDictionaryClasss as part of a transaction rollback of Release().
    /// </summary>
    internal static void RollbackItemRelease(IStorageItem item) {
      var needsDictionaryClass = (NeedsDictionaryClass) item;
      DC.Data._NeedsDictionaryClasssByName.Add(needsDictionaryClass.Name, needsDictionaryClass);
      needsDictionaryClass.onRollbackItemRelease();
    }
    partial void onRollbackItemRelease();


    /// <summary>
    /// Returns property values for tracing. Parents are shown with their key instead their content.
    /// </summary>
    public string ToTraceString() {
      var returnString =
        $"{this.GetKeyOrHash()}|" +
        $" {Name}|" +
        $" {Address}";
      onToTraceString(ref returnString);
      return returnString;
    }
    partial void onToTraceString(ref string returnString);


    /// <summary>
    /// Returns property values
    /// </summary>
    public string ToShortString() {
      var returnString =
        $"{Key.ToKeyString()}," +
        $" {Name}," +
        $" {Address}";
      onToShortString(ref returnString);
      return returnString;
    }
    partial void onToShortString(ref string returnString);


    /// <summary>
    /// Returns all property names and values
    /// </summary>
    public override string ToString() {
      var returnString =
        $"Key: {Key.ToKeyString()}," +
        $" Name: {Name}," +
        $" Address: {Address};";
      onToString(ref returnString);
      return returnString;
    }
    partial void onToString(ref string returnString);
    #endregion
  }
}
